# DiscordBot

Ein discord.js v14 Bot für Render.

## Deployment auf Render
1. Repository mit Render verbinden.
2. Der Blueprint (`render.yaml`) wird automatisch erkannt.
3. Den echten Bot-Token im Render Dashboard unter der Umgebungsvariable `TOKEN` eintragen.
